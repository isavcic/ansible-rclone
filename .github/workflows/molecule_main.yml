---

name: molecule_main

on:   # yamllint disable-line rule:truthy
  push:
    branches:
      - master
      - ci_*
      - os_packages
    paths-ignore:
      - 'README.md'
  pull_request:
    branches:
      - master

jobs:
  molecule:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scenario:
          - default
        distros:
          - centos7
          - centos8
          - debian9
          - debian10
          - fedora31
          - fedora32
          - ubuntu1804
          - ubuntu2004
        versions:
          - stable
        os_packages:
          - true
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"
      - name: molecule_main
        uses: robertdebock/molecule-action@2.6.3
        env:
          MOLECULE_DISTRO: "${{ matrix.distros }}"
          TEST_VERSION: "${{ matrix.versions }}"
          OS_PACKAGES: "${{ matrix.os_packages }}"
        with:
          options: parallel
          scenario: ${{ matrix.scenario }}
